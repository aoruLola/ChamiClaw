# ChamiClaw 路线图 / 计划 / TODO

更新时间：2026-02-14 (UTC+8)

---

## 1. Roadmap（8 周）

### Phase 0：准备期（第 0 周，1-2 天）
**目标**：确定工程骨架、配置规范、日志标准。  
**交付**：
- 目录结构初始化
- `config.yaml` 与 `.env.example`
- `schema.sql`（SQLite）
- JSON 结构化日志字段规范

**验收标准**：
- 本地可启动空服务并连通 SQLite
- 日志统一 UTC，字段完整率 100%

---

### Phase 1：数据层（第 1-2 周）
**目标**：稳定采集可交易市场与行情。  
**交付**：
- 市场扫描器（活跃市场抓取 + 过滤）
- 行情采集器（bid/ask/mid、深度、成交）
- 规则摘要（规则不清晰默认禁入）
- 入库：`markets`、`quotes`

**验收标准**：
- 连续运行 72h 无致命中断
- 数据缺口率 < 1%
- 每个市场具备规则状态字段

---

### Phase 2：信号与风控 MVP（第 3-4 周）
**目标**：以结构信号为主，双 LLM 为辅。  
**交付**：
- 结构信号：YES+NO 偏离、期限结构异常（先）
- 双LLM：LLM1 产出 fair_prob，LLM2 给 confidence + 校验
- Risk Engine：`risk.check(order_intent)` 强制门禁

**验收标准**：
- 每个信号可追溯（输入/输出/原因）
- 风控拒单日志完整率 100%

---

### Phase 3：执行与对账（第 5-6 周）
**目标**：形成“可控执行”闭环。  
**交付**：
- 订单状态机（限价、超时撤单、最小改价间隔）
- 幂等防重（`signal_id`）
- 对账任务（交易所成交/持仓 vs 本地）
- 状态机：`RUNNING / PAUSED / HALTED`

**验收标准**：
- 重复下单 0 次
- 对账差异可自动修复或自动暂停
- 异常 1 分钟内告警

---

### Phase 4：评估与校准（第 7-8 周）
**目标**：策略可量化迭代。  
**交付**：
- 回测引擎（quotes 重放 + 简化撮合）
- Paper Trading 评估（5m / 30m / 24h）
- 概率校准（分桶 + isotonic/Platt）
- 策略版本化 + 日报/周报

**验收标准**：
- 自动输出日报
- 参数变更可关联收益/回撤变化

---

## 2. 执行计划（优先级）

### P0（必须先做）
1. 配置与密钥隔离
2. SQLite Schema 与索引
3. JSON 日志规范
4. 市场扫描 + 行情采集
5. Risk Engine 骨架

### P1（核心能力）
6. 结构信号引擎
7. 双LLM推理接口（含超时降级）
8. 订单管理（限价优先 + 幂等）
9. 对账与状态机

### P2（增强）
10. 回测与模拟交易
11. 校准与仓位联动
12. 监控告警看板

---

## 3. TODO 清单

### A. 工程基础
- [x] 初始化 `src/` 模块结构（scanner/signal/risk/execution/reconcile/evaluate）
- [x] 新建 `config/config.yaml`、`.env.example`
- [x] 编写 `sql/schema.sql`
- [x] 完成 JSONL 日志中间件（UTC）

### B. 数据层
- [ ] 市场扫描（活跃市场 + 流动性过滤 + 到期窗口）
- [ ] 行情采集（bid/ask/mid/depth/trades）
- [ ] 规则摘要（结算时间/数据源/歧义点）
- [ ] 入库去重与重试

### C. 信号层
- [ ] 结构信号 v1（YES+NO 偏离、期限结构）
- [ ] LLM1 输出规范：`fair_prob/reason`
- [ ] LLM2 输出规范：`confidence/risk_tags`
- [ ] 统一 edge/cost 计算器

### D. 风控与执行
- [ ] 实现 `risk.check(order_intent)`
- [ ] 限价下单 + 超时撤单 + 改价间隔
- [ ] `signal_id` 幂等键与防重逻辑
- [ ] 对账修正 + 异常自动 PAUSED

### E. 评估与校准
- [ ] 回测器（简化撮合）
- [ ] Paper Trading 记录器
- [ ] 校准模块（分桶 + isotonic）
- [ ] 策略版本记录 + 日报生成

### F. 运维与安全
- [x] 告警（Discord/Webhook）
- [x] RUNNING/PAUSED/HALTED 状态控制
- [x] 密钥隔离（研究层不可见私钥）
- [x] 故障演练（API 失败/对账异常/回撤超限，新增 `drill` 命令支持 dry-run 与状态落盘）

---

## 4. 风险控制红线（执行前必须启用）

- 单市场仓位上限：1-2%
- 同类事件总敞口：≤10%
- 单日最大回撤触发 HALTED
- 大 spread 禁止成交
- 到期前限制加仓
- 任何订单必须先过 `risk.check`

---

## 5. 里程碑交付件

- M1（第2周）：数据层可持续运行 + 基础报表
- M2（第4周）：信号层 + 风控门禁可回放
- M3（第6周）：执行/对账闭环 + 断路器
- M4（第8周）：回测/校准/版本化报告
